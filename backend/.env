# Configuración del servidor Backend
HOST=0.0.0.0
PORT=4000
NODE_ENV=development

# Origen permitido para CORS (Frontend)
# Puedes especificar múltiples orígenes separados por coma si es necesario
FRONTEND_ORIGIN=http://localhost:5173,http://127.0.0.1:5173

# Conexión a Base de Datos MySQL (en la máquina host local)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=Solarflare2020*
DB_NAME=recetario_db

# Secretos JWT
JWT_SECRET=super_secret_jwt_key_recetario_gemini_2026_983749817234
JWT_EXPIRES_IN=7d
